<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Address - ShopHub</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar">
    <div class="container navbar-container">
      <a href="/products" class="navbar-brand">🛍️ ShopHub</a>
      <ul class="navbar-nav">
        <li><a href="/products" class="nav-link">Products</a></li>
        <li><a href="/cart" class="nav-link">Cart</a></li>
        <li><a href="/orders" class="nav-link">Orders</a></li>
        <li><a href="/profile" class="nav-link active">Profile</a></li>
        <li><a href="/auth/logout" class="nav-link">Logout</a></li>
      </ul>
    </div>
  </nav>

  <!-- Main Content -->
  <div class="profile-page">
    <div class="container container-md">
      <!-- Breadcrumb -->
      <div class="mb-lg" style="display: flex; align-items: center; gap: var(--spacing-sm); font-size: 0.875rem;">
        <a href="/profile" class="link">Profile</a>
        <span style="color: var(--gray-400);">/</span>
        <a href="/profile/addresses" class="link">Addresses</a>
        <span style="color: var(--gray-400);">/</span>
        <span style="color: var(--gray-600);">Add New</span>
      </div>

      <!-- Page Header -->
      <div class="mb-xl">
        <h1>Add New Address</h1>
        <p style="color: var(--gray-600); margin: 0;">Fill in your delivery address details</p>
      </div>

      <!-- Form Card -->
      <div class="card">
        <div class="card-body">
          <form method="POST" action="/profile/addresses">
            <!-- Address Line -->
            <div class="form-group">
              <label for="address" class="form-label required">Street Address</label>
              <input
                type="text"
                id="address"
                name="address"
                class="form-input"
                placeholder="House/Flat No., Building Name, Street"
                required
                autofocus
              />
              <span class="form-hint">Include landmarks for easy delivery</span>
            </div>

            <!-- District & State -->
            <div class="grid grid-cols-2">
              <div class="form-group">
                <label for="district" class="form-label required">District</label>
                <input
                  type="text"
                  id="district"
                  name="district"
                  class="form-input"
                  placeholder="Enter district"
                  required
                />
              </div>

              <div class="form-group">
                <label for="state" class="form-label required">State</label>
                <input
                  type="text"
                  id="state"
                  name="state"
                  class="form-input"
                  placeholder="Enter state"
                  required
                />
              </div>
            </div>

            <!-- Pincode -->
            <div class="form-group">
              <label for="pincode" class="form-label required">PIN Code</label>
              <input
                type="text"
                id="pincode"
                name="pincode"
                class="form-input"
                placeholder="6-digit PIN code"
                pattern="[0-9]{6}"
                maxlength="6"
                required
              />
              <span class="form-hint">6-digit postal code</span>
            </div>

            <!-- Address Type (Optional Enhancement) -->
            <div class="form-group">
              <label class="form-label">Address Type</label>
              <div style="display: flex; gap: var(--spacing-md); flex-wrap: wrap;">
                <label style="display: flex; align-items: center; gap: var(--spacing-sm); cursor: pointer; padding: var(--spacing-md) var(--spacing-lg); border: 2px solid var(--gray-300); border-radius: var(--radius-md); transition: all var(--transition-base);">
                  <input type="radio" name="address_type" value="home" checked style="width: auto;">
                  <span>🏠 Home</span>
                </label>
                <label style="display: flex; align-items: center; gap: var(--spacing-sm); cursor: pointer; padding: var(--spacing-md) var(--spacing-lg); border: 2px solid var(--gray-300); border-radius: var(--radius-md); transition: all var(--transition-base);">
                  <input type="radio" name="address_type" value="work" style="width: auto;">
                  <span>💼 Work</span>
                </label>
                <label style="display: flex; align-items: center; gap: var(--spacing-sm); cursor: pointer; padding: var(--spacing-md) var(--spacing-lg); border: 2px solid var(--gray-300); border-radius: var(--radius-md); transition: all var(--transition-base);">
                  <input type="radio" name="address_type" value="other" style="width: auto;">
                  <span>📍 Other</span>
                </label>
              </div>
            </div>

            <!-- Info Box -->
            <div class="alert alert-info">
              <span>ℹ️</span>
              <div>
                <strong>Delivery Information</strong>
                <p style="margin: 0; font-size: 0.875rem;">Make sure all details are correct to ensure smooth delivery of your orders.</p>
              </div>
            </div>

            <!-- Form Actions -->
            <div style="display: flex; gap: var(--spacing-md); justify-content: flex-end; margin-top: var(--spacing-xl);">
              <a href="/profile/addresses" class="btn btn-secondary">
                Cancel
              </a>
              <button type="submit" class="btn btn-primary">
                Save Address
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <style>
    /* Radio button styling */
    input[type="radio"]:checked + span {
      font-weight: 600;
    }

    label:has(input[type="radio"]:checked) {
      border-color: var(--primary) !important;
      background: rgba(37, 99, 235, 0.05);
    }

    label:has(input[type="radio"]):hover {
      border-color: var(--primary-light);
      background: rgba(37, 99, 235, 0.03);
    }

    @media (max-width: 768px) {
      .grid-cols-2 {
        grid-template-columns: 1fr !important;
      }

      div[style*="justify-content: flex-end"] {
        flex-direction: column-reverse;
      }

      div[style*="justify-content: flex-end"] .btn {
        width: 100%;
      }
    }
  </style>
</body>
</html>
