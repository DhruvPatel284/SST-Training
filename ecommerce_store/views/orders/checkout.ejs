<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Checkout - ShopHub</title>
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar">
    <div class="container navbar-container">
      <a href="/products" class="navbar-brand">🛍️ ShopHub</a>
      <ul class="navbar-nav">
        <li><a href="/products" class="nav-link">Products</a></li>
        <li><a href="/cart" class="nav-link">🛒 Cart</a></li>
        <li><a href="/orders" class="nav-link">Orders</a></li>
        <li><a href="/profile" class="nav-link">Profile</a></li>
        <li><a href="/auth/logout" class="nav-link">Logout</a></li>
      </ul>
    </div>
  </nav>

  <!-- Main Content -->
  <div style="padding: var(--spacing-2xl) 0; background: var(--gray-50); min-height: calc(100vh - 80px);">
    <div class="container container-lg">
      <!-- Progress Steps -->
      <div class="mb-xl">
        <div style="display: flex; justify-content: center; align-items: center; gap: var(--spacing-md); margin-bottom: var(--spacing-lg);">
          <div style="display: flex; align-items: center; gap: var(--spacing-sm);">
            <div style="width: 40px; height: 40px; border-radius: 50%; background: var(--success); color: white; display: flex; align-items: center; justify-content: center; font-weight: 700;">✓</div>
            <span style="font-weight: 600; color: var(--success);">Cart</span>
          </div>
          <div style="width: 60px; height: 2px; background: var(--primary);"></div>
          <div style="display: flex; align-items: center; gap: var(--spacing-sm);">
            <div style="width: 40px; height: 40px; border-radius: 50%; background: var(--primary); color: white; display: flex; align-items: center; justify-content: center; font-weight: 700;">2</div>
            <span style="font-weight: 600; color: var(--primary);">Checkout</span>
          </div>
          <div style="width: 60px; height: 2px; background: var(--gray-300);"></div>
          <div style="display: flex; align-items: center; gap: var(--spacing-sm);">
            <div style="width: 40px; height: 40px; border-radius: 50%; background: var(--gray-300); color: var(--gray-600); display: flex; align-items: center; justify-content: center; font-weight: 700;">3</div>
            <span style="font-weight: 600; color: var(--gray-600);">Confirmation</span>
          </div>
        </div>
        <h1 class="text-center">Checkout</h1>
        <p class="text-center" style="color: var(--gray-600); margin: 0;">
          <% if (singleItem) { %>
            Quick checkout for 1 item
          <% } else { %>
            Review your order and complete purchase
          <% } %>
        </p>
      </div>

      <form method="POST" action="/orders" id="checkoutForm">
        <div class="grid" style="grid-template-columns: 1fr 400px; gap: var(--spacing-xl); align-items: start;">
          <!-- Left Column - Address Selection -->
          <div>
            <!-- Delivery Address -->
            <div class="card mb-xl">
              <div class="card-header">
                <h2 class="card-title">1. Delivery Address</h2>
              </div>
              <div class="card-body">
                <% if (addresses.length === 0) { %>
                  <div class="alert alert-warning">
                    <span>⚠️</span>
                    <div>
                      <strong>No addresses found</strong>
                      <p style="margin: 0; font-size: 0.875rem;">Please add a delivery address before placing an order.</p>
                    </div>
                  </div>
                  <a href="/profile/addresses/new" class="btn btn-primary mt-md">
                    + Add Address
                  </a>
                <% } else { %>
                  <div style="display: flex; flex-direction: column; gap: var(--spacing-md);">
                    <% addresses.forEach((address, index) => { %>
                      <label class="address-card" for="address-<%= address.id %>">
                        <input
                          type="radio"
                          id="address-<%= address.id %>"
                          name="addressId"
                          value="<%= address.id %>"
                          <%= index === 0 ? 'checked' : '' %>
                          required
                        />
                        <div class="address-content">
                          <div style="display: flex; justify-content: space-between; align-items: start; margin-bottom: var(--spacing-sm);">
                            <div>
                              <strong style="font-size: 1.125rem;">Address <%= index + 1 %></strong>
                              <% if (index === 0) { %>
                                <span class="badge badge-primary" style="margin-left: var(--spacing-sm);">Default</span>
                              <% } %>
                            </div>
                            <div class="radio-check">✓</div>
                          </div>
                          <p style="margin: 0; color: var(--gray-700); line-height: 1.8;">
                            <%= address.address %><br>
                            <%= address.district %>, <%= address.state %><br>
                            PIN: <strong><%= address.pincode %></strong>
                          </p>
                        </div>
                      </label>
                    <% }) %>
                  </div>

                  <div style="margin-top: var(--spacing-lg); padding-top: var(--spacing-lg); border-top: 1px solid var(--gray-200);">
                    <a href="/profile/addresses/new" class="link">+ Add New Address</a>
                  </div>
                <% } %>
              </div>
            </div>

            <!-- Order Items Review -->
            <div class="card">
              <div class="card-header">
                <h2 class="card-title">
                  2. Review Items 
                  <span style="color: var(--gray-500); font-weight: 400;">(<%= cartItems.length %> item<%= cartItems.length > 1 ? 's' : '' %>)</span>
                </h2>
              </div>
              <div class="card-body" style="padding: 0;">
                <div class="list-group" style="box-shadow: none; border-radius: 0;">
                  <% cartItems.forEach((item, index) => { %>
                    <div class="list-group-item" style="display: block;">
                      <div style="display: grid; grid-template-columns: 80px 1fr auto; gap: var(--spacing-md); align-items: center;">
                        <!-- Product Image -->
                        <div style="width: 80px; height: 80px; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; font-size: 2rem;">
                          <%= item.product.category === 'Electronics' ? '📱' : 
                              item.product.category === 'Fashion' ? '👕' : 
                              item.product.category === 'Home' ? '🏠' : 
                              item.product.category === 'Books' ? '📚' : 
                              item.product.category === 'Sports' ? '⚽' : '🛍️' %>
                        </div>

                        <!-- Product Info -->
                        <div>
                          <span class="badge badge-primary" style="font-size: 0.75rem; margin-bottom: var(--spacing-xs);">
                            <%= item.product.category %>
                          </span>
                          <h4 style="font-size: 1rem; margin-bottom: var(--spacing-xs);">
                            <%= item.product.name %>
                          </h4>
                          <p style="font-size: 0.875rem; color: var(--gray-600); margin: 0;">
                            Qty: <%= item.quantity %> × ₹<%= item.product.price.toLocaleString() %>
                          </p>
                        </div>

                        <!-- Price -->
                        <div style="text-align: right;">
                          <div style="font-size: 1.25rem; font-weight: 700; color: var(--gray-900);">
                            ₹<%= (item.product.price * item.quantity).toLocaleString() %>
                          </div>
                        </div>
                      </div>
                    </div>
                  <% }) %>
                </div>
              </div>
            </div>

            <!-- Hidden Form Fields -->
            <% if (singleItem) { %>
              <input type="hidden" name="singleItem" value="true" />
              <input type="hidden" name="cartItemId" value="<%= cartItemId %>" />
            <% } else { %>
              <input type="hidden" name="singleItem" value="false" />
            <% } %>
          </div>

          <!-- Right Column - Order Summary -->
          <div>
            <div class="card" style="position: sticky; top: 80px;">
              <div class="card-header">
                <h3 class="card-title">Order Summary</h3>
              </div>
              <div class="card-body">
                <!-- Price Breakdown -->
                <div style="display: flex; flex-direction: column; gap: var(--spacing-md); margin-bottom: var(--spacing-lg);">
                  <div style="display: flex; justify-content: space-between;">
                    <span style="color: var(--gray-600);">Subtotal</span>
                    <span style="font-weight: 600;">₹<%= total.toLocaleString() %></span>
                  </div>
                </div>

                <div class="divider" style="margin: var(--spacing-lg) 0;"></div>

                <!-- Total -->
                <div style="display: flex; justify-content: space-between; margin-bottom: var(--spacing-xl);">
                  <span style="font-size: 1.25rem; font-weight: 700;">Total Amount</span>
                  <span style="font-size: 1.75rem; font-weight: 700; color: var(--primary);">
                    ₹<%= total %>
                  </span>
                </div>

                <!-- Place Order Button -->
                <button 
                  type="submit" 
                  class="btn btn-primary btn-lg btn-block"
                  <%= addresses.length === 0 ? 'disabled' : '' %>
                >
                  <% if (addresses.length === 0) { %>
                    Add Address First
                  <% } else { %>
                    Place Order
                  <% } %>
                </button>                
              </div>
            </div>

            <!-- Cancel Button -->
            <div class="mt-lg text-center">
              <a href="<%= singleItem ? '/cart' : '/cart' %>" class="link">
                ← Back to Cart
              </a>
            </div>
          </div>
        </div>
      </form>
    </div>
  </div>

  <style>
    .address-card {
      display: block;
      padding: var(--spacing-lg);
      border: 2px solid var(--gray-300);
      border-radius: var(--radius-lg);
      cursor: pointer;
      transition: all var(--transition-base);
      position: relative;
    }

    .address-card:hover {
      border-color: var(--primary-light);
      background: var(--gray-50);
    }

    .address-card input[type="radio"] {
      position: absolute;
      opacity: 0;
      pointer-events: none;
    }

    .address-card input[type="radio"]:checked + .address-content {
      /* Styles applied when checked */
    }

    .address-card input[type="radio"]:checked ~ .radio-check {
      display: flex;
    }

    .address-card:has(input[type="radio"]:checked) {
      border-color: var(--primary);
      background: rgba(37, 99, 235, 0.05);
    }

    .radio-check {
      display: none;
      width: 28px;
      height: 28px;
      border-radius: 50%;
      background: var(--primary);
      color: white;
      align-items: center;
      justify-content: center;
      font-size: 0.875rem;
      font-weight: 700;
    }

    @media (max-width: 768px) {
      .grid[style*="grid-template-columns: 1fr 400px"] {
        grid-template-columns: 1fr !important;
      }

      div[style*="grid-template-columns: 80px 1fr auto"] {
        grid-template-columns: 60px 1fr !important;
      }

      .card[style*="position: sticky"] {
        position: static !important;
      }

      div[style*="display: flex"][style*="justify-content: center"] {
        flex-wrap: wrap;
        gap: var(--spacing-sm) !important;
      }

      div[style*="width: 60px; height: 2px"] {
        width: 30px !important;
      }
    }
  </style>

  <script>
    document.getElementById('checkoutForm').addEventListener('submit', function(e) {
      const selectedAddress = document.querySelector('input[name="addressId"]:checked');
      if (!selectedAddress) {
        e.preventDefault();
        alert('Please select a delivery address');
        return false;
      }
    });
  </script>
</body>
</html>
